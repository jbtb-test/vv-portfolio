cat > .gitignore << 'EOF'
# ----------------------------------------
# vv-portfolio — Git ignore
# ----------------------------------------

# Node tooling (used only to render Mermaid diagrams)
node_modules/
package.json
package-lock.json

# Editor / OS artifacts
.~lock.*#
.DS_Store
Thumbs.db
EOF
